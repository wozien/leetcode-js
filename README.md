💪💪💪 Fight with algorithm

## leetcode vscode config

```json
{
  "leetcode.defaultLanguage": "javascript",
  "leetcode.filePath": {
    "default": {
      "folder": "${tag}",
      "filename": "${id}.${kebab-case-name}.${ext}"
    }
  }
}
```